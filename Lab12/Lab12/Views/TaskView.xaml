<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lab12.Views.TaskView">
    <ContentPage.Content>
        <StackLayout Padding="50">
            <Entry FontSize="Title" Text="{Binding Title}" Placeholder="Title"></Entry>
            <Entry FontSize="Title" Text="{Binding Description}" Placeholder="Description"></Entry>
            <!--ComboBox-->
            <Entry FontSize="Title" Text="{Binding Status}" Placeholder="Status"></Entry>
            <Button FontSize="Title" Command="{Binding SaveCommand}" Text="Save"></Button>
            <Button FontSize="Title" Command="{Binding ListCommand}" Text="List"></Button>
            <ListView x:Name="TaskListView" ItemsSource="{Binding Tasks}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Title}" Detail="{Binding Description}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>